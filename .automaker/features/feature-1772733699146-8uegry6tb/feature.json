{
  "category": "Uncategorized",
  "description": "## Context\nDepends on: sanitizeStreamingMarkdown helper and plugin memoization fix (ship that first).\n\nTwo render-layer changes to the message component:\n\n## Part 1: Gate syntax highlighting\nDuring streaming, rehype-highlight re-tokenizes the entire code block on every token. This is expensive and looks noisy. Disable it while streaming, apply on completion.\n\n```tsx\nconst REMARK_PLUGINS = [remarkGfm]; // already fixed by dependency feature\nconst REHYPE_PLUGINS_STREAMING: [] = [];\nconst REHYPE_PLUGINS_COMPLETE = [rehypeHighlight];\n\n<ReactMarkdown\n  remarkPlugins={REMARK_PLUGINS}\n  rehypePlugins={isComplete ? REHYPE_PLUGINS_COMPLETE : REHYPE_PLUGINS_STREAMING}\n>\n  {isComplete ? content : sanitizeStreamingMarkdown(content)}\n</ReactMarkdown>\n```\n\n## Part 2: Wire sanitizeStreamingMarkdown\nImport and apply `sanitizeStreamingMarkdown` from `apps/ui/src/lib/streaming-markdown.ts` to the content passed to ReactMarkdown while `isComplete === false`.\n\nFind the message render component in `apps/ui/src/` (likely in `components/views/` or a chat-specific directory) and apply both changes.\n\n## Acceptance Criteria\n- rehype-highlight is not in the rehype plugin array while isComplete is false\n- sanitizeStreamingMarkdown is applied to content during streaming\n- Syntax highlighting renders correctly on stream completion\n- No TypeScript errors",
  "title": "Gate rehype-highlight behind isComplete + wire sanitizeStreamingMarkdown into message renderer",
  "priority": 3,
  "status": "backlog",
  "id": "feature-1772733699146-8uegry6tb",
  "branchName": "feature/gate-rehype-highlight-behind-iscomplete",
  "descriptionHistory": [
    {
      "description": "## Context\nDepends on: sanitizeStreamingMarkdown helper and plugin memoization fix (ship that first).\n\nTwo render-layer changes to the message component:\n\n## Part 1: Gate syntax highlighting\nDuring streaming, rehype-highlight re-tokenizes the entire code block on every token. This is expensive and looks noisy. Disable it while streaming, apply on completion.\n\n```tsx\nconst REMARK_PLUGINS = [remarkGfm]; // already fixed by dependency feature\nconst REHYPE_PLUGINS_STREAMING: [] = [];\nconst REHYPE_PLUGINS_COMPLETE = [rehypeHighlight];\n\n<ReactMarkdown\n  remarkPlugins={REMARK_PLUGINS}\n  rehypePlugins={isComplete ? REHYPE_PLUGINS_COMPLETE : REHYPE_PLUGINS_STREAMING}\n>\n  {isComplete ? content : sanitizeStreamingMarkdown(content)}\n</ReactMarkdown>\n```\n\n## Part 2: Wire sanitizeStreamingMarkdown\nImport and apply `sanitizeStreamingMarkdown` from `apps/ui/src/lib/streaming-markdown.ts` to the content passed to ReactMarkdown while `isComplete === false`.\n\nFind the message render component in `apps/ui/src/` (likely in `components/views/` or a chat-specific directory) and apply both changes.\n\n## Acceptance Criteria\n- rehype-highlight is not in the rehype plugin array while isComplete is false\n- sanitizeStreamingMarkdown is applied to content during streaming\n- Syntax highlighting renders correctly on stream completion\n- No TypeScript errors",
      "timestamp": "2026-03-05T18:01:39.151Z",
      "source": "initial"
    }
  ],
  "createdAt": "2026-03-05T18:01:39.151Z",
  "statusHistory": [
    {
      "from": null,
      "to": "backlog",
      "timestamp": "2026-03-05T18:01:39.151Z",
      "reason": "Feature created"
    }
  ],
  "featureType": "code",
  "dependencies": [
    "feature-1772733692445-4to4sjr3r"
  ]
}