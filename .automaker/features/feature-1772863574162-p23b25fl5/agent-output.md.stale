I'll start by reading the relevant files to understand the existing patterns.

🔧 Tool: ToolSearch
Input: {
  "query": "select:Read,Bash,Write,Edit,Glob,Grep",
  "max_results": 6
}

🔧 Tool: Read
Input: {
  "file_path": "/Users/kj/dev/automaker/apps/server/src/routes/projects/index.ts"
}

🔧 Tool: Bash
Input: {
  "command": "ls /Users/kj/dev/automaker/apps/server/tests/integration/routes/ 2>/dev/null || echo \"Directory does not exist\"",
  "description": "Check if integration test directory exists"
}

🔧 Tool: Bash
Input: {
  "command": "ls /Users/kj/dev/automaker/libs/types/src/ 2>/dev/null | head -30",
  "description": "List types in libs/types"
}

🔧 Tool: Read
Input: {
  "file_path": "/Users/kj/dev/automaker/apps/server/tests/integration/routes/project-summary.test.ts"
}

🔧 Tool: Bash
Input: {
  "command": "ls /Users/kj/dev/automaker/apps/server/src/routes/projects/routes/",
  "description": "List project route files"
}

🔧 Tool: Bash
Input: {
  "command": "ls /Users/kj/dev/automaker/libs/types/src/ | grep -E \"project|summary|milestone\"",
  "description": "Find project-related type files"
}
Good - `summary.ts` already exists. Let me read it and the related services/types.

🔧 Tool: Read
Input: {
  "file_path": "/Users/kj/dev/automaker/apps/server/src/routes/projects/routes/summary.ts"
}

🔧 Tool: Read
Input: {
  "file_path": "/Users/kj/dev/automaker/libs/types/src/project.ts",
  "limit": 100
}

🔧 Tool: Read
Input: {
  "file_path": "/Users/kj/dev/automaker/apps/server/src/routes/projects/routes/timeline.ts"
}
Now let me check if `ProjectSummary` and `MilestoneSummary` types exist in the types package, and also the `index.ts` for the types library.

🔧 Tool: Grep
Input: {
  "pattern": "ProjectSummary|MilestoneSummary",
  "path": "/Users/kj/dev/automaker/libs/types/src",
  "type": "ts",
  "output_mode": "content"
}

🔧 Tool: Bash
Input: {
  "command": "grep -n \"ProjectSummary\\|MilestoneSummary\\|ArtifactIndexEntry\" /Users/kj/dev/automaker/libs/types/src/index.ts 2>/dev/null | head -20",
  "description": "Check if types are exported from index"
}
Let me check if the summary route is wired in `index.ts` and then run the tests.

🔧 Tool: Bash
Input: {
  "command": "cd /Users/kj/dev/automaker && npx vitest run apps/server/tests/integration/routes/project-summary.test.ts 2>&1 | tail -40",
  "description": "Run project summary integration tests"
}
Tests pass. Now I need to wire the summary route in `index.ts`. Looking at the imports, `createSummaryHandler` is already imported but the route is not registered. Let me add it:

🔧 Tool: Edit
Input: {
  "replace_all": false,
  "file_path": "/Users/kj/dev/automaker/apps/server/src/routes/projects/index.ts",
  "old_string": "  // Timeline route — GET /api/projects/:slug/timeline\n  if (eventLedgerService) {\n    router.get('/:slug/timeline', createTimelineHandler(eventLedgerService));\n  }",
  "new_string": "  // Timeline route — GET /api/projects/:slug/timeline\n  if (eventLedgerService) {\n    router.get('/:slug/timeline', createTimelineHandler(eventLedgerService));\n  }\n\n  // Summary route — GET /api/projects/:slug/summary\n  if (eventLedgerService) {\n    router.get(\n      '/:slug/summary',\n      createSummaryHandler(projectService, projectArtifactService, eventLedgerService)\n    );\n  }"
}
