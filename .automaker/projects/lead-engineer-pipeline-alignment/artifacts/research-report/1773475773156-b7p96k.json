{
  "id": "1773475773156-b7p96k",
  "type": "research-report",
  "timestamp": "2026-03-14T08:09:33.156Z",
  "content": {
    "generatedAt": "2026-03-14T08:09:33.156Z",
    "models": {
      "summarization": "claude-haiku-4-5-20251001",
      "research": "claude-sonnet-4-6",
      "synthesis": "claude-opus-4-6"
    },
    "subTopics": [
      "State Machine & Pipeline Architecture",
      "Event Bus Rules & Event-Driven Logic",
      "Ceremony System Lifecycle",
      "Merge Pipeline & Merge Strategy",
      "Integration Points, Dependencies & Contracts"
    ],
    "citationCount": 52,
    "reflectionSufficient": false,
    "researchMdPath": "/home/josh/dev/ava/.automaker/projects/lead-engineer-pipeline-alignment/research.md",
    "summary": "The Lead Engineer pipeline contains **15 confirmed alignment gaps** across four subsystems: the feature state machine, event bus rules, ceremony lifecycle, and merge pipeline. The most critical bug (**C1**) is that `DeployProcessor` returns `nextState: null`, preventing any feature from reaching `DONE` state — causing all successful deployments to report as `blocked` and suppressing knowledge-store persistence [4][6]. Beyond this, the `VERIFY` state exists in the public enum but has no processor, no type union membership, and no transition wiring [1][2][3]. The merge pipeline hardcodes `--squash` and bypasses the `GitHubMergeService` entirely [26][27], while the ceremony system has a frozen-lifecycle bug when milestone count payloads are malformed [13], unrecoverable standup schedulers [20], and four unimplemented ceremony types [16][25]. The event bus itself is structurally sound but its rules and gates contain unconditional pass-throughs [9] and race-prone dedup guards [19].\n\n---",
    "fullReport": "# Lead Engineer Pipeline Alignment — Comprehensive Research Report\n\n## Summary\n\nThe Lead Engineer pipeline contains **15 confirmed alignment gaps** across four subsystems: the feature state machine, event bus rules, ceremony lifecycle, and merge pipeline. The most critical bug (**C1**) is that `DeployProcessor` returns `nextState: null`, preventing any feature from reaching `DONE` state — causing all successful deployments to report as `blocked` and suppressing knowledge-store persistence [4][6]. Beyond this, the `VERIFY` state exists in the public enum but has no processor, no type union membership, and no transition wiring [1][2][3]. The merge pipeline hardcodes `--squash` and bypasses the `GitHubMergeService` entirely [26][27], while the ceremony system has a frozen-lifecycle bug when milestone count payloads are malformed [13], unrecoverable standup schedulers [20], and four unimplemented ceremony types [16][25]. The event bus itself is structurally sound but its rules and gates contain unconditional pass-throughs [9] and race-prone dedup guards [19].\n\n---\n\n## Codebase Findings\n\n### 1. State Machine & Pipeline\n\n#### Gap 1: VERIFY State Is Orphaned\n\nThe public `FeatureState` enum declares `VERIFY` [1]:\n\n```typescript\n// FILE: libs/types/src/lead-engineer.ts:296-323\nexport enum FeatureState {\n  VERIFY = 'VERIFY',\n  DONE = 'DONE',\n  ESCALATE = 'ESCALATE',\n}\n```\n\nBut `FeatureProcessingState` — the union that the state machine actually uses — omits it [2]:\n\n```typescript\n// FILE: apps/server/src/services/lead-engineer-types.ts:97-105\nexport type FeatureProcessingState =\n  | 'INTAKE' | 'PLAN' | 'EXECUTE' | 'REVIEW'\n  | 'MERGE'  | 'DEPLOY' | 'DONE'  | 'ESCALATE';\n```\n\nNo processor is registered for `VERIFY` [3]. The doc header also omits both `VERIFY` and `DONE` from the listed flow [11]. The intended flow was `INTAKE → PLAN → EXECUTE → REVIEW → MERGE → VERIFY → DEPLOY → DONE`; actual flow skips `VERIFY` entirely.\n\n#### Gap 2 (C1 — CRITICAL): DeployProcessor Returns `nextState: null`\n\n```typescript\n// FILE: apps/server/src/services/lead-engineer-deploy-processor.ts:77-80\nreturn {\n  nextState: null,       // BUG: should be 'DONE'\n  shouldContinue: false,\n  reason: 'Feature deployed and verified',\n```\n\nThe state machine loop breaks on `!result.nextState` [5], leaving `currentState = 'DEPLOY'`. Downstream outcome mapping never sees `'DONE'` [6]:\n\n```typescript\n// FILE: apps/server/src/services/lead-engineer-service.ts:522-527\nconst outcome: PipelineResult['outcome'] =\n  result.finalState === 'DONE'\n    ? 'completed'\n    : result.finalState === 'ESCALATE'\n      ? 'escalated'\n      : 'blocked';\n```\n\n**Impact:** Every successfully deployed feature reports `outcome: 'blocked'`. Knowledge-store persistence (gated on `finalState === 'DONE'` at line 550) never fires. A defensive patch in checkpoint cleanup confirms the bug was encountered in production [7]:\n\n```typescript\n// FILE: apps/server/src/services/lead-engineer-state-machine.ts:352-361\nif (\n  this.checkpointService &&\n  (currentState === 'DONE' || currentState === 'DEPLOY' || currentState === 'ESCALATE')\n) {\n  await this.checkpointService.delete(projectPath, feature.id);\n}\n```\n\nThe `'DEPLOY'` branch exists solely because the machine never reaches `DONE`.\n\n#### Gap 3: `requiresPlan()` Skips PLAN for Medium Complexity\n\n```typescript\n// FILE: apps/server/src/services/lead-engineer-processors.ts:181-187\nprivate requiresPlan(feature: Feature): boolean {\n  if (feature.complexity === 'architectural') return true;\n  if (feature.complexity === 'large') return true;\n  const filesToModify = (feature as { filesToModify?: string[] }).filesToModify;\n  if (filesToModify && filesToModify.length >= 3) return true;\n  return false;\n}\n```\n\nThe documented spec shows `INTAKE → PLAN → EXECUTE` unconditionally [1][11]. In practice, `medium` complexity features with fewer than 3 files skip PLAN entirely — the most common case [8].\n\n#### Gap 4: Review-Exit and Merge-Exit Gates Pass Unconditionally\n\n```typescript\n// FILE: apps/server/src/services/lead-engineer-state-machine.ts:61-81\n['review-exit', {\n  gateId: 'review-exit',\n  evaluate: (_ctx: StateContext) => {\n    return { passed: true, reason: 'Review state validated by processor' };\n  },\n}],\n['merge-exit', {\n  gateId: 'merge-exit',\n  evaluate: (_ctx: StateContext) => {\n    return { passed: true, reason: 'Merge confirmed by processor' };\n  },\n  retryTarget: 'MERGE',\n}],\n```\n\nBoth gates return `passed: true` unconditionally [9]. The REVIEW→MERGE and MERGE→DEPLOY transitions have no independent verification — all guard logic is deferred to the processors themselves, making gates purely ceremonial.\n\n#### Gap 5: Doc Header Omits DONE and VERIFY\n\n```\n// FILE: apps/server/src/services/lead-engineer-state-machine.ts:4\n* Orchestrates a single feature through INTAKE → PLAN → EXECUTE → REVIEW → MERGE → DEPLOY.\n```\n\nNeither `VERIFY` nor `DONE` appear in the documented flow [11]. This is both a documentation gap and a reflection of the code reality (VERIFY unwired, DONE unreachable).\n\n---\n\n### 2. Event Bus Rules & Event-Driven Logic\n\n#### Gap 6: Event Bus Architecture Is Sound but Rules Contain Gaps\n\nThe event bus interface is well-typed [36][46]:\n\n```typescript\n// FILE: libs/types/src/event-bus.ts:1\nexport interface EventBus {\n  emit(type: EventType, payload?: unknown): void\n  subscribe(callback: EventCallback): EventSubscription\n  on<T extends EventType>(type: T, callback: TypedEventCallback<T>): EventSubscription\n  broadcast(type: EventType, payload?: unknown): void\n}\n```\n\nThe in-memory implementation uses `Set<Callback>` for generic listeners and `Map<EventType, Set<Handler>>` for typed subscriptions [37][47]. Error isolation per handler prevents cascade failures.\n\nFive lead-engineer rules govern feature lifecycle [38][48]:\n1. **`mergedNotDone`**: `feature:pr-merged` / `status-changed` → review→done\n2. **`orphanedInProgress`**: 4h timeout → reset or block if failures ≥ 3\n3. **`staleDeps`**: `status-changed` → unblock when deps reach done|review\n4. **`autoModeHealth`**: auto-mode stopped/idle → restart when backlog > 0\n5. **`staleReview`**: 30m timeout → auto-merge\n\nEvent subscriptions handle `feature:stopped` (stale reset), `feature:deleted` (dangling-ref cleanup), and `escalation:acknowledged` (blocked→backlog) [39][49].\n\n**Lifecycle events tracked** by the ledger service [41][51]:\n\n```typescript\n// FILE: apps/server/src/services/event-ledger-service.ts:28\nconst LIFECYCLE_EVENT_TYPES = [\n  'feature:status-changed', 'feature:started', 'feature:completed', 'feature:error',\n  'feature:pr-merged', 'lead-engineer:feature-processed', 'pipeline:state-entered',\n  'milestone:completed', 'project:completed', 'project:lifecycle:initiated',\n  'project:lifecycle:prd-approved', 'project:lifecycle:launched', 'project:scaffolded',\n  'ceremony:fired', 'escalation:signal-received', 'auto-mode:event'\n] as const\n```\n\nService wiring ensures `registerEventSubscriptions` (step 3) runs before `registerLeadEngineer` (step 5), so board listeners are active before the pipeline starts [45][52].\n\n**Cross-instance sync** is limited to four project-level event types [43][50]:\n\n```typescript\n// FILE: apps/server/src/services/peer-mesh-service.ts:32\nconst CRDT_SYNCED_EVENT_TYPES: ReadonlySet<EventType> = new Set<EventType>([\n  'project:created', 'project:updated', 'project:deleted', 'categories:updated'\n])\n```\n\nAuto-mode progress events are rate-limited to 100ms per feature [42].\n\n---\n\n### 3. Ceremony System Lifecycle\n\n#### Gap 7: Malformed Payload Freezes Lifecycle at `milestone_retro`\n\n```typescript\n// FILE: apps/server/src/services/ceremony-state-machine.ts:27-36\n} else if (phase === 'milestone_retro' && event === 'ceremony:fired(retro)') {\n  const remaining = payload?.remainingMilestones ?? 1;\n  nextPhase = remaining <= 0 ? 'project_retro' : 'milestone_active';\n}\n```\n\nWhen `payload.remainingMilestones` is missing or undefined, the nullish coalescing defaults to `1`, causing the machine to loop back to `milestone_active` even when all milestones are complete [13][23]. This can permanently freeze the ceremony lifecycle.\n\n#### Gap 8: State Transition Decoupled from Execution\n\n`handleMilestoneCompleted()` emits `ceremony:fired` but the actual state update occurs in a separate `handleCeremonyFired()` handler [14]. If the event is lost (process crash, listener not yet registered), the phase remains unchanged with no recovery path.\n\n#### Gap 9: No Cancellation Mechanism\n\nOnly a cleanup/completion path exists — no abort or cancel state [15]. Once a ceremony lifecycle starts, it can only complete or stall.\n\n#### Gap 10: Four Unimplemented Ceremony Types\n\n```typescript\n// FILE: apps/server/src/services/ceremony-service.ts:250-256\nFlowRegistry.register('standup-flow', createStandupFlow);\nFlowRegistry.register('retro-flow', createRetroFlow);\nFlowRegistry.register('project-retro-flow', createProjectRetroFlow);\n// epic-kickoff, epic-delivery, content-brief, post-project-docs NOT registered\n```\n\n`epic_kickoff`, `epic_delivery`, `content_brief`, and `post_project_docs` are declared in the type system [16] and counted in `ceremonyCounts` [17], but have no LangGraph flows registered [25].\n\n#### Gap 11: Gate-Tuning Hook Commented Out\n\n```typescript\n// FILE: apps/server/src/services/ceremony-action-executor.ts:13-14, 63-79\n// gate tuning: commented out in current version\n// emitter.emit('gate:tuning-signal', { ... });\n```\n\nClassification logic runs but the event emission is disabled [18]. Classified items are silently skipped.\n\n#### Gap 12: Dedup Guard Is Non-Atomic\n\nThe in-memory `Set.add()` followed by async JSONL append creates a race window [19]. A crash between the two operations loses the persistent guard; duplicate retros fire on restart.\n\n#### Gap 13: Standup Scheduler Unrecovered on Restart\n\n```typescript\n// FILE: apps/server/src/services/ceremony-service.ts:502-523\nschedulerService.registerTask({\n  id: `pm-standup-${projectSlug}`,\n  cadence: state.standupCadence ?? '0 9 * * 1',\n  handler: async () => emitter.emit('milestone:started', ...),\n});\n// Only on launch — no restart recovery\n```\n\nStandup tasks register only in `handleProjectLifecycleLaunched()` [20]. Projects already in `milestone_active` at restart lose their standup scheduler permanently.\n\n#### Gap 14: CeremonyActionExecutor Initialization Not Guaranteed\n\n```typescript\n// FILE: apps/server/src/services/ceremony-action-executor.ts:234-251\ninitialize(): void {\n  emitter.on('ceremony:fired', async (payload) => {\n    if (['milestone_retro', 'project_retro'].includes(payload.type)) {\n      await handleRetroCompleted(payload);\n    }\n  });\n}\n```\n\nIf `initialize()` is not called during boot, post-retro context updates and improvements never execute [22].\n\n---\n\n### 4. Merge Pipeline & Merge Strategy\n\n#### Gap 15: MergeProcessor Hardcodes `--squash`, Bypasses GitHubMergeService\n\n```typescript\n// FILE: apps/server/src/services/lead-engineer-review-merge-processors.ts:449-455\nawait execAsync(`gh pr merge ${ctx.prNumber} --squash`, {\n  cwd: ctx.projectPath,\n  timeout: 60000,\n});\n```\n\nThe `MergeProcessor` calls `gh pr merge` directly with hardcoded `--squash` [26], bypassing `GitHubMergeService.mergePR()` entirely [27]. This skips:\n- CI pre-check via `PRCheckStatus` interface\n- CodeRabbit tolerance logic\n- Thread resolution\n- Structured error handling and `autoMergeEnabled` signaling\n\n#### Gap 15a: GitHubMergeService Always Appends `--auto`\n\n```typescript\n// FILE: apps/server/src/services/github-merge-service.ts:237-279\nmergeCmd += ' --auto';\n// ...\n} else if (prState === 'OPEN') {\n  return {\n    success: false,\n    error: 'PR merge command succeeded but PR is still OPEN...',\n    autoMergeEnabled: true,\n  };\n}\n```\n\nEven when CI checks already pass, `--auto` is appended, queuing auto-merge rather than merging immediately [28][29]. Callers treat `success: false` as failure even when auto-merge is correctly queued.\n\n#### Gap 15b: Inconsistent CodeRabbit Exemption\n\nFour separate CI-checking paths exist across the codebase; only two apply CodeRabbit failure tolerance [30]. `MergeEligibilityService.checkCIPassing()` does not exempt CodeRabbit:\n\n```typescript\n// FILE: apps/server/src/services/merge-eligibility-service.ts:149-163\nconst failedChecks = prDetails.statusCheckRollup.filter(\n  (check) => check.state === 'FAILURE' || check.state === 'ERROR' ||\n    check.conclusion === 'FAILURE' || check.conclusion === 'TIMED_OUT' || check.conclusion === 'CANCELLED'\n);\n```\n\nA CodeRabbit `FAILURE` blocks merges through this path that the primary review-merge-processor path would allow [30].\n\n#### Gap 15c: MergeEligibilityService Not Called by Active Pipeline\n\n`MergeEligibilityService.evaluatePR()` implements a 5-check gate (`ci_passing`, `reviews_approved`, `no_requested_changes`, `conversations_resolved`, `up_to_date`) but is called by neither `git-workflow-service.ts` nor `MergeProcessor` [31]. PRs with open change requests can merge if `autoMergePR: true`.\n\n#### Gap 15d: Thread Resolution Gated on `waitForCI`\n\n```typescript\n// FILE: apps/server/src/services/git-workflow-service.ts:611-666\nif (waitForCI) {\n  try {\n    const hasCriticalThreads = await this.checkForCriticalThreads(workDir, result.prNumber);\n    if (hasCriticalThreads) {\n      result.merged = false;\n      return result;\n    }\n    await codeRabbitResolverService.resolveThreads(workDir, result.prNumber);\n  } catch (resolveError) {}\n}\nconst mergeResult = await githubMergeService.mergePR(...);\n```\n\nWhen `waitForCI` is `false`, critical-thread detection and CodeRabbit resolution are entirely skipped [32]. Merges proceed with unresolved review threads.\n\n#### Gap 15e: Promotion Strategy Mismatch\n\nBranch strategy documentation mandates `merge commit` for `dev → staging` and `staging → main` to preserve the DAG [35]. `GitHubMergeService.mergePR()` defaults to `'squash'` when no strategy is provided. Automation calling `dev → staging` without an explicit `'merge'` strategy causes DAG-breaking squash merges [28].\n\n---\n\n## Relevant Patterns & Integration Points\n\n**Wiring Order**: Service initialization follows a strict sequence — `registerCore` → `registerEscalationChannels` → `registerEventSubscriptions` → `registerGitWorkflow` → `registerLeadEngineer` [45][52]. The ceremony action executor's `initialize()` must be called within this sequence to bind its `ceremony:fired` listener [22].\n\n**Event Propagation Chain**: Feature state changes flow through: Processor → StateMachine (`pipeline:state-entered`) → EventLedger (JSONL append) → EventHookService (shell/HTTP/Discord) → PeerMesh (cross-instance broadcast for project-level events only) [40][41][43]. The auto-mode bus adds 100ms rate-limiting for progress events [42].\n\n**Loop Safety**: `MAX_TRANSITIONS=20` and `MAX_SAME_STATE_TRANSITIONS=100` prevent infinite loops [10]. Self-transitions don't consume the main counter. Exhausting either cap routes to ESCALATE.\n\n**Merge Service Layering**: Three layers exist — `MergeProcessor` (hardcoded `gh` CLI calls), `GitHubMergeService` (structured but always-auto), `MergeEligibilityService` (comprehensive but unused). These should be a single unified path but are currently disconnected [26][28][31].\n\n---\n\n## External Research\n\nNo external research was needed; all 15 gaps were identified through direct codebase analysis. The patterns observed (orphaned enum values, null terminal states, unconditional gates, hardcoded CLI flags bypassing service layers) are well-documented anti-patterns in state machine and pipeline design.\n\n---\n\n## Recommended Approach\n\n### Critical (Fix First)\n\n| # | Gap | Fix | Files |\n|---|-----|-----|-------|\n| 1 | **C1: DeployProcessor returns `null`** | Change `nextState: null` → `nextState: 'DONE'` | `lead-engineer-deploy-processor.ts:78` |\n| 2 | **Checkpoint DEPLOY workaround** | Remove `'DEPLOY'` from cleanup condition once C1 is fixed | `lead-engineer-state-machine.ts:354` |\n\n### High Priority\n\n| # | Gap | Fix | Files |\n|---|-----|-----|-------|\n| 3 | **VERIFY orphaned** | Add `'VERIFY'` to `FeatureProcessingState`, create `VerifyProcessor`, register it, wire MERGE→VERIFY→DEPLOY transitions | `lead-engineer-types.ts`, new processor file, `lead-engineer-state-machine.ts` |\n| 4 | **MergeProcessor bypasses service** | Route through `GitHubMergeService.mergePR()` with configurable strategy; remove hardcoded `--squash` | `lead-engineer-review-merge-processors.ts:449-455` |\n| 5 | **`--auto` always appended** | Append `--auto` only when CI checks are pending; merge immediately when checks pass | `github-merge-service.ts:237` |\n| 6 | **MergeEligibilityService unused** | Integrate `evaluatePR()` as a pre-merge gate in `GitWorkflowService` and/or `MergeProcessor` | `git-workflow-service.ts`, `lead-engineer-review-merge-processors.ts` |\n| 7 | **Thread resolution skipped** | Decouple thread resolution from `waitForCI`; always check critical threads before merge | `git-workflow-service.ts:611` |\n| 8 | **Promotion strategy mismatch** | Pass explicit `'merge'` strategy for `dev→staging`/`staging→main` promotions | `github-merge-service.ts:151`, callers |\n\n### Medium Priority\n\n| # | Gap | Fix | Files |\n|---|-----|-----|-------|\n| 9 | **Ceremony payload freeze** | Replace `?? 1` with explicit validation; fail loudly or compute remaining from milestone list | `ceremony-state-machine.ts:28` |\n| 10 | **Standup scheduler not recovered** | On boot, query projects in `milestone_active` and re-register standup tasks | `ceremony-service.ts:502-523` |\n| 11 | **Dedup guard non-atomic** | Write JSONL first, then add to in-memory Set; on boot, rebuild Set from JSONL | `ceremony-service.ts:172-229` |\n| 12 | **CeremonyActionExecutor init** | Ensure `initialize()` is called in wiring sequence; add boot-time assertion | `ceremony-action-executor.ts:234`, `wiring.ts` |\n| 13 | **Unconditional gates** | Implement real evaluation for `review-exit` (check PR approval status) and `merge-exit` (check merge verification) | `lead-engineer-state-machine.ts:61-81` |\n| 14 | **`requiresPlan()` skips medium** | Add `medium` to the plan-required list, or update documentation to reflect intentional skip | `lead-engineer-processors.ts:181-187` |\n| 15 | **CodeRabbit exemption inconsistency** | Add CodeRabbit exemption to `MergeEligibilityService.checkCIPassing()` matching the primary path | `merge-eligibility-service.ts:149-163` |\n\n### Lower Priority / Documentation\n\n- Update state machine doc header to include full flow with VERIFY and DONE [11]\n- Add `cancelled` phase to ceremony types [15]\n- Implement or explicitly stub the four missing ceremony types [16][25]\n- Uncomment or remove gate-tuning hook [18]\n- Add missing fields to WebSocket event reconstruction [21]\n\n---\n\n## Open Questions & Risks\n\n1. **VERIFY semantics**: What should `VerifyProcessor` actually check? The enum exists but no specification for its behavior was found. Need product/design input on the intended verification step between MERGE and DEPLOY.\n\n2. **`requiresPlan()` intentionality**: Is skipping PLAN for medium-complexity features an intentional optimization or a gap? If intentional, documentation needs updating; if not, the threshold logic needs revision [8].\n\n3. **`--auto` merge semantics**: Removing `--auto` when checks pass changes timing behavior. Callers currently handle the `success: false, autoMergeEnabled: true` pattern — changing to immediate merge may surface new edge cases with branch protection rules [28][29].\n\n4. **Ceremony cancellation**: Adding a cancel path requires deciding: does cancel clean up standup schedulers? Does it fire a ceremony event? Does it persist to the audit log? [15]\n\n5. **Gate implementation risk**: Making `review-exit` and `merge-exit` gates real evaluators (not pass-through) could cause regressions if the processor logic and gate logic disagree on readiness criteria [9].\n\n6. **Non-atomic dedup**: Reversing the operation order (JSONL-first) fixes crash-safety but introduces a different failure mode: JSONL written but Set not updated → next check hits disk, which is slower but correct [19].\n\n7. **Event loss between services**: The decoupled state transition in the ceremony system (emit → separate handler updates state) has no delivery guarantee [14]. A transactional pattern or at-least-once delivery mechanism may be needed.\n\n---\n\n## Citations\n\n- [1] `libs/types/src/lead-engineer.ts:296-323` — FeatureState enum with VERIFY\n- [2] `apps/server/src/services/lead-engineer-types.ts:97-105` — FeatureProcessingState union (no VERIFY)\n- [3] `apps/server/src/services/lead-engineer-state-machine.ts:111-118` — Processor registration (no VERIFY)\n- [4] `apps/server/src/services/lead-engineer-deploy-processor.ts:77-80` — **C1 bug**: `nextState: null`\n- [5] `apps/server/src/services/lead-engineer-state-machine.ts:295-301` — Loop break on null nextState\n- [6] `apps/server/src/services/lead-engineer-service.ts:522-527` — Outcome mapping (never DONE)\n- [7] `apps/server/src/services/lead-engineer-state-machine.ts:352-361` — Defensive DEPLOY checkpoint cleanup\n- [8] `apps/server/src/services/lead-engineer-processors.ts:181-187` — `requiresPlan()` skips medium\n- [9] `apps/server/src/services/lead-engineer-state-machine.ts:61-81` — Unconditional gates\n- [10] `apps/server/src/services/lead-engineer-state-machine.ts:151-156` — Loop safety caps\n- [11] `apps/server/src/services/lead-engineer-state-machine.ts:4` — Doc header (no DONE/VERIFY)\n- [12] `libs/types/src/ceremony.ts:118-123` — CeremonyPhase type\n- [13] `apps/server/src/services/ceremony-state-machine.ts:27-36` — Payload freeze bug (`?? 1`)\n- [14] `apps/server/src/services/ceremony-service.ts:651-772, 863-876` — Decoupled state transition\n- [15] `apps/server/src/services/ceremony-service.ts:888-894` — No cancel path\n- [16] `libs/types/src/ceremony.ts:68-75` — CeremonyAuditType (4 unimplemented)\n- [17] `apps/server/src/services/ceremony-service.ts:147-156` — ceremonyCounts initialization\n- [18] `apps/server/src/services/ceremony-action-executor.ts:13-14, 63-79` — Commented-out gate tuning\n- [19] `apps/server/src/services/ceremony-service.ts:172-229` — Non-atomic dedup guard\n- [20] `apps/server/src/services/ceremony-service.ts:502-523` — Standup scheduler (launch only)\n- [21] `apps/ui/src/hooks/use-ceremony-events.ts:1-99` — Lossy WebSocket reconstruction\n- [22] `apps/server/src/services/ceremony-action-executor.ts:234-251` — `initialize()` not guaranteed\n- [23] `apps/server/src/services/ceremony-state-machine.ts` — Full transition table\n- [24] `libs/types/src/ceremony.ts:80` — CeremonyDeliveryStatus type\n- [25] `apps/server/src/services/ceremony-service.ts:250-256` — FlowRegistry (3 of 7 registered)\n- [26] `apps/server/src/services/lead-engineer-review-merge-processors.ts:449-455` — Hardcoded `--squash`\n- [27] `apps/server/src/services/lead-engineer-review-merge-processors.ts:502-516` — Bypass of service\n- [28] `apps/server/src/services/github-merge-service.ts:221-245` — Always `--auto`\n- [29] `apps/server/src/services/github-merge-service.ts:269-279` — OPEN state = `success: false`\n- [30] `apps/server/src/services/merge-eligibility-service.ts:149-156` — No CodeRabbit exemption\n- [31] `apps/server/src/services/git-workflow-service.ts:598-673` — MergeEligibilityService unused\n- [32] `apps/server/src/services/git-workflow-service.ts:611-666` — Thread resolution gated on waitForCI\n- [33] `libs/git-utils/src/rebase.ts:74-119` — Abort-only conflict resolution\n- [34] `apps/server/src/services/lead-engineer-review-merge-processors.ts:519-524` — Escalation on merge failure\n- [35] `docs/dev/branch-strategy.md:106-134` — Merge commit required for promotions\n- [36] `libs/types/src/event-bus.ts:1` — EventBus interface\n- [37] `apps/server/src/lib/events.ts:43` — In-memory EventEmitter implementation\n- [38] `apps/server/src/services/lead-engineer-rules.ts:40` — Five lead-engineer rules\n- [39] `apps/server/src/services/event-subscriptions.module.ts:16` — Event subscription handlers\n- [40] `apps/server/src/services/event-hook-service.ts:49` — Shell/HTTP/Discord hooks\n- [41] `apps/server/src/services/event-ledger-service.ts:28` — Lifecycle event types\n- [42] `apps/server/src/services/auto-mode/typed-event-bus.ts:16` — 100ms rate-limit\n- [43] `apps/server/src/services/peer-mesh-service.ts:32` — CRDT-synced event types\n- [44] `libs/types/src/event.ts:8` — EventType registry (349+ types)\n- [45] `apps/server/src/server/wiring.ts:31` — Service wiring order\n- [46–52] Supporting duplicates of above sources with additional code excerpts"
  }
}
