{
  "slug": "project-level-agent-extensions",
  "title": "Project-Level Agent Extensions",
  "goal": "Make agent roles extensible at the project level — discoverable from .automaker/agents/*.yml manifests, wired into execution (model selection + prompt injection), with auto-assignment match rules and a settings UI.",
  "status": "completed",
  "milestones": [
    {
      "number": 1,
      "slug": "01-type-foundation",
      "title": "Type Foundation",
      "description": "Make AgentRole extensible and define the agent manifest data model. This is the foundation — all other milestones depend on these types.",
      "phases": [
        {
          "number": 1,
          "name": "extensible-agentrole-type-projectagent-manifest-types",
          "title": "Extensible AgentRole type + ProjectAgent manifest types",
          "description": "1. Change AgentRole from union type to `string` with a BUILT_IN_AGENT_ROLES const array for the 8 existing roles. Update ROLE_CAPABILITIES to use Record<string, RoleCapabilities> instead of Record<AgentRole, ...>. This keeps all existing code working while allowing arbitrary role strings.\n\n2. Create new file libs/types/src/agent-manifest.ts with:\n- ProjectAgent interface: name, extends (base role), description, model override, promptFile path, capabilities (partial RoleCapabilities), match rules (categories, keywords, filePatterns)\n- AgentManifest interface: agents array, version\n- AgentMatchRules interface: categories string[], keywords string[], filePatterns string[]\n- DEFAULT_PROJECT_AGENT constant for sensible defaults\n\n3. Export from libs/types/src/index.ts barrel.\n\n4. Update feature.ts: assignedRole type stays string (already compatible since AgentRole becomes string).\n\nFiles to modify:\n- libs/types/src/agent-roles.ts\n- libs/types/src/agent-manifest.ts (new)\n- libs/types/src/index.ts",
          "filesToModify": [
            "libs/types/src/agent-roles.ts",
            "libs/types/src/agent-manifest.ts",
            "libs/types/src/index.ts"
          ],
          "acceptanceCriteria": [
            "AgentRole is string type, BUILT_IN_AGENT_ROLES has the 8 existing roles",
            "ROLE_CAPABILITIES keyed by string, existing role lookups unchanged",
            "ProjectAgent, AgentManifest, AgentMatchRules types exported from @protolabsai/types",
            "npm run build:packages succeeds",
            "npm run test:packages passes (existing tests unbroken)"
          ],
          "complexity": "medium",
          "featureId": "feature-1773426322470-026o4hqex"
        },
        {
          "number": 2,
          "name": "per-project-agent-config-in-workflowsettings",
          "title": "Per-project agent config in WorkflowSettings",
          "description": "Add an optional agentConfig section to WorkflowSettings (per-project .automaker/settings.json) that allows per-role model overrides and default agent assignment behavior.\n\nAdd to WorkflowSettings interface:\n- agentConfig?: { roleModelOverrides?: Record<string, PhaseModelEntry>; autoAssignEnabled?: boolean; manifestPaths?: string[] }\n\nroleModelOverrides maps role name (string) to model config. autoAssignEnabled controls whether match rules run (default: true). manifestPaths allows custom manifest locations beyond the default .automaker/agents/.\n\nUpdate DEFAULT_WORKFLOW_SETTINGS to not include agentConfig (undefined = use defaults).\n\nFiles to modify:\n- libs/types/src/workflow-settings.ts",
          "filesToModify": [
            "libs/types/src/workflow-settings.ts"
          ],
          "acceptanceCriteria": [
            "agentConfig is optional on WorkflowSettings",
            "roleModelOverrides maps string role names to PhaseModelEntry",
            "autoAssignEnabled defaults to true when agentConfig is present",
            "npm run build:packages succeeds",
            "No changes to DEFAULT_WORKFLOW_SETTINGS beyond the new optional field"
          ],
          "complexity": "small",
          "featureId": "feature-1773426322472-ihp92miv5"
        }
      ],
      "status": "completed",
      "epicId": "feature-1773426322467-htb5m3530"
    },
    {
      "number": 2,
      "slug": "02-agent-manifest-service",
      "title": "Agent Manifest Service",
      "description": "Server-side service that discovers, parses, validates, and caches agent manifests from .automaker/agents/. Exposes via API for the UI.",
      "phases": [
        {
          "number": 1,
          "name": "agentmanifestservice-load-parse-validate-cache",
          "title": "AgentManifestService — load, parse, validate, cache",
          "description": "Create apps/server/src/services/agent-manifest-service.ts as a singleton service.\n\nResponsibilities:\n1. loadManifest(projectPath): Discovers .automaker/agents.yml (single file) OR .automaker/agents/*.yml (directory of files). Parses YAML using the js-yaml package (already a dependency via other packages in the monorepo — verify before adding). Validates against ProjectAgent type. Returns AgentManifest.\n\n2. getAgentsForProject(projectPath): Returns cached manifest or loads fresh. Cache key is projectPath. Cache invalidation on file change (fs.watch on the agents directory).\n\n3. getAgent(projectPath, roleName): Lookup a specific agent by name. Returns ProjectAgent | undefined.\n\n4. getResolvedCapabilities(projectPath, roleName): Merges project agent capabilities with its base role (extends field) from ROLE_CAPABILITIES. Returns full RoleCapabilities.\n\n5. matchFeature(projectPath, feature): Runs all project agents' match rules against a feature's category, title, description, and filesToModify. Returns the best-matching ProjectAgent or null.\n\nRegister as singleton in the service registry pattern used by other services.\n\nIMPORTANT: Check if js-yaml is already a dependency in the monorepo. If not, use a simple YAML parser or JSON with comments. The constraint is no NEW npm dependencies — but js-yaml may already exist.\n\nFiles to create:\n- apps/server/src/services/agent-manifest-service.ts\n\nFiles to modify:\n- apps/server/src/services/index.ts (register in service exports if barrel exists)",
          "filesToModify": [
            "apps/server/src/services/agent-manifest-service.ts",
            "apps/server/src/services/index.ts"
          ],
          "acceptanceCriteria": [
            "loadManifest reads .automaker/agents.yml or .automaker/agents/*.yml",
            "Validates parsed YAML against ProjectAgent schema",
            "Caches per projectPath with file-watch invalidation",
            "matchFeature checks categories, keywords, filePatterns",
            "getResolvedCapabilities merges with base role from ROLE_CAPABILITIES",
            "Unit tests cover: load single file, load directory, match rules, cache invalidation"
          ],
          "complexity": "medium",
          "featureId": "feature-1773426322476-ekxrdymvz"
        },
        {
          "number": 2,
          "name": "api-routes-for-agent-manifest",
          "title": "API routes for agent manifest",
          "description": "Add Express routes to expose discovered agents:\n\n1. POST /api/agents/list — returns all agents (built-in + project) for a given projectPath\n2. POST /api/agents/get — returns a single agent by name with resolved capabilities\n3. POST /api/agents/match — given a feature ID, returns the best-matching agent\n\nAll routes require projectPath in body. Built-in roles are always included in the list response, merged with any project manifest overrides.\n\nCreate route file following existing patterns in apps/server/src/routes/.\n\nFiles to create:\n- apps/server/src/routes/agents.ts\n\nFiles to modify:\n- apps/server/src/routes/index.ts (register routes)",
          "filesToModify": [
            "apps/server/src/routes/agents.ts",
            "apps/server/src/routes/index.ts"
          ],
          "acceptanceCriteria": [
            "POST /api/agents/list returns built-in + project agents",
            "POST /api/agents/get returns single agent with resolved capabilities",
            "POST /api/agents/match runs match rules against a feature",
            "Routes follow existing Express route patterns",
            "Error handling for missing projectPath, invalid agent names"
          ],
          "complexity": "medium",
          "featureId": "feature-1773426322478-pdjiu2oj3"
        }
      ],
      "status": "completed",
      "epicId": "feature-1773426322474-qy22t3gbk"
    },
    {
      "number": 3,
      "slug": "03-execution-wiring",
      "title": "Execution Wiring",
      "description": "Wire the agent manifest into the actual execution path — model selection, prompt injection, and auto-assignment on feature pickup.",
      "phases": [
        {
          "number": 1,
          "name": "wire-assignedrole-into-getmodelforfeature",
          "title": "Wire assignedRole into getModelForFeature()",
          "description": "Modify getModelForFeature() in apps/server/src/services/auto-mode/execution-service.ts to check the assigned role's model override BEFORE falling back to complexity-based selection.\n\nNew priority chain:\n1. feature.model (explicit per-feature override) — unchanged\n2. Failure escalation (2+ failures → opus) — unchanged\n3. Architectural complexity → opus — unchanged\n4. **NEW: assignedRole model override** — if feature.assignedRole is set, check:\n   a. AgentManifestService.getAgent(projectPath, assignedRole) for manifest model override\n   b. WorkflowSettings.agentConfig.roleModelOverrides[assignedRole] for settings override\n5. phaseModels.agentExecutionModel from settings — unchanged\n6. Complexity fallback — unchanged\n\nAlso check roleModelOverrides from WorkflowSettings (per-project settings.json). The manifest model takes precedence over settings if both exist, but settings override allows users to override manifest defaults without editing YAML.\n\nFiles to modify:\n- apps/server/src/services/auto-mode/execution-service.ts",
          "filesToModify": [
            "apps/server/src/services/auto-mode/execution-service.ts"
          ],
          "acceptanceCriteria": [
            "getModelForFeature checks assignedRole manifest model after failure escalation",
            "WorkflowSettings roleModelOverrides checked as fallback after manifest",
            "Existing priority chain (explicit model, failure escalation, complexity) unchanged",
            "Features without assignedRole behave exactly as before",
            "Unit tests cover: role with manifest model, role with settings override, role with both, unknown role fallback"
          ],
          "complexity": "medium",
          "featureId": "feature-1773426322486-bkuknxhvo"
        },
        {
          "number": 2,
          "name": "role-prompt-injection-in-agent-system-prompt",
          "title": "Role prompt injection in agent system prompt",
          "description": "When a feature has an assignedRole with a prompt file defined in the manifest, load that prompt file and prepend it to the agent's system prompt.\n\nModify the prompt construction path in execution-service.ts where contextFilesPrompt is built. After loading .automaker/context/*.md files, also check if the feature's assignedRole has a promptFile in the manifest. If so, read the file and prepend it as a role-specific instruction block.\n\nPrompt injection format:\n```\n## Agent Role: {agent.name}\n{agent.description}\n\n{contents of agent.promptFile}\n\n---\n{existing context files}\n{existing system prompt}\n```\n\nThe promptFile path is relative to projectPath (e.g., .claude/agents/kai.md or .automaker/agents/prompts/kai.md).\n\nFiles to modify:\n- apps/server/src/services/auto-mode/execution-service.ts",
          "filesToModify": [
            "apps/server/src/services/auto-mode/execution-service.ts"
          ],
          "acceptanceCriteria": [
            "Role prompt file loaded when assignedRole has promptFile in manifest",
            "Prompt prepended before context files in system prompt",
            "Missing promptFile logs warning but doesn't break execution",
            "Features without assignedRole or without promptFile work unchanged",
            "Prompt injection follows the format: role header + prompt content + separator"
          ],
          "complexity": "medium",
          "featureId": "feature-1773426322491-48i1j2xd7"
        },
        {
          "number": 3,
          "name": "match-rule-auto-assignment-on-feature-pickup",
          "title": "Match rule auto-assignment on feature pickup",
          "description": "Before auto-mode picks up a feature for execution, run match rules to auto-assign a role if none is set.\n\nModify the feature pickup path in execution-service.ts (or auto-mode-service.ts where features are dequeued). After selecting a feature but before calling getModelForFeature():\n\n1. If feature.assignedRole is already set → skip (respect manual assignment)\n2. If WorkflowSettings.agentConfig?.autoAssignEnabled === false → skip\n3. Call AgentManifestService.matchFeature(projectPath, feature)\n4. If a match is found with confidence above threshold:\n   a. Set feature.assignedRole = match.name\n   b. Set feature.routingSuggestion = { role: match.name, confidence, reasoning, autoAssigned: true, suggestedAt }\n   c. Persist via featureLoader.update()\n5. Proceed with execution (getModelForFeature will now pick up the assigned role)\n\nMatch rule evaluation order:\n1. Category exact match (highest confidence)\n2. Keyword match in title/description (medium confidence)\n3. File pattern match against filesToModify (medium confidence)\n4. Multiple matches → pick highest combined confidence\n\nFiles to modify:\n- apps/server/src/services/auto-mode/execution-service.ts",
          "filesToModify": [
            "apps/server/src/services/auto-mode/execution-service.ts"
          ],
          "acceptanceCriteria": [
            "Features without assignedRole get auto-matched before execution",
            "Manual assignedRole is never overwritten",
            "autoAssignEnabled=false disables match rule evaluation",
            "routingSuggestion populated with match details",
            "Features with no matching rules proceed with default behavior",
            "Unit tests cover: category match, keyword match, file pattern match, no match, manual override respected"
          ],
          "complexity": "medium",
          "featureId": "feature-1773426322496-9hrre00sh"
        }
      ],
      "status": "completed",
      "epicId": "feature-1773426322482-darnxu2q6"
    },
    {
      "number": 4,
      "slug": "04-settings-ui",
      "title": "Settings UI",
      "description": "Frontend UI for viewing discovered agents and assigning roles to features.",
      "phases": [
        {
          "number": 1,
          "name": "agents-panel-in-project-settings-feature-role-selector",
          "title": "Agents panel in project settings + feature role selector",
          "description": "Two UI additions:\n\n1. **Project Settings — Agents Panel**: Add an Agents section to the project settings view. Shows a list of discovered project agents (from /api/agents/list) with:\n- Agent name, description, base role (extends), model override\n- Match rules summary (categories, keywords)\n- Whether auto-assignment is enabled (toggle)\n\n2. **Feature Card — Role Selector**: In the feature detail/edit view, add a dropdown for assignedRole that shows:\n- Built-in roles (8 defaults)\n- Project agents (from manifest)\n- Current assignment highlighted\n- 'Auto' option that clears assignedRole (lets match rules decide)\n\nUse existing UI patterns: Select component from @protolabsai/ui/atoms, same Card/PropertyRow patterns as project-sidebar.tsx.\n\nAdd React Query hook for fetching agents list.\n\nFiles to create:\n- apps/ui/src/components/views/projects-view/components/agents-panel.tsx\n- apps/ui/src/hooks/use-agents.ts\n\nFiles to modify:\n- apps/ui/src/components/views/projects-view/project-settings-view.tsx (or equivalent settings page — add Agents panel)\n- Feature detail component (add role selector)",
          "filesToModify": [
            "apps/ui/src/components/views/projects-view/components/agents-panel.tsx",
            "apps/ui/src/hooks/use-agents.ts",
            "apps/ui/src/components/views/projects-view/project-settings-view.tsx"
          ],
          "acceptanceCriteria": [
            "Agents panel shows built-in + project agents in settings view",
            "Feature card has assignedRole dropdown with all available roles",
            "Auto option clears assignedRole for match-rule-based assignment",
            "React Query hook fetches from /api/agents/list",
            "No new UI dependencies — uses existing atoms/patterns"
          ],
          "complexity": "medium",
          "featureId": "feature-1773426322499-6ocibn4fz"
        }
      ],
      "status": "pending",
      "epicId": "feature-1773426322497-f7jd6v1zq"
    }
  ],
  "prd": {
    "situation": "protoLabs Studio has 8 hardcoded AgentRole values defined as a TypeScript union type in libs/types/src/agent-roles.ts. CLI personas (Matt, Kai, Sam, Jon) exist as .claude/commands/*.md files but are user-invoked only. The assignedRole field on features is wired to this union but isn't enforced during execution — getModelForFeature() in execution-service.ts looks at complexity and failure count, never at the assigned role. No mechanism exists to load a role-specific prompt or model override from a project-level manifest.",
    "problem": "1. AgentRole is a compile-time union — adding project-specific roles requires rebuilding the types package.\n2. Auto-mode ignores assignedRole entirely during model selection and prompt construction.\n3. No runtime agent manifest loading from .automaker/agents/.\n4. Features can't be auto-assigned to specialized agents based on category/keyword/file patterns.\n5. Model defaults are global — no per-project or per-role overrides in the execution path.",
    "approach": "M1 — Type Foundation: Change AgentRole from a union to string with BUILT_IN_AGENT_ROLES as a const array. Add ProjectAgent and AgentManifest types. Add agentConfig to per-project workflow settings.\n\nM2 — Manifest Service: AgentManifestService discovers and parses .automaker/agents.yml + .automaker/agents/*.yml at project load. Exposes agents via API route. Caches per project with file-watch invalidation.\n\nM3 — Execution Wiring: getModelForFeature() checks assigned role's manifest entry for model override. Agent system prompt loader checks for role-specific prompt file and prepends it. Match rules run before auto-mode pickup to auto-assign assignedRole.\n\nM4 — Settings UI: Project settings Agents tab shows discovered project agents. Feature card exposes assignedRole selector with built-in + discovered options.",
    "results": "- assignedRole becomes meaningful in execution — drives model selection and prompt injection\n- Project-level agents discoverable from .automaker/agents/*.yml manifests\n- Auto-assignment via match rules (category, keywords, file patterns)\n- Backward compatible — all existing features and built-in roles unchanged\n- Foundation for RFC-001 Phase 2+ (quality gates, agent groups) without rework",
    "constraints": [
      "No new npm dependencies",
      "Zero breaking changes to existing feature JSON or settings",
      "Must work with the existing 8 built-in roles unchanged",
      "No quality gates or agent groups in this scope (deferred to future RFC phases)",
      "Manifest format: YAML with comments support",
      "AgentRole type must remain assignable from plain strings for backward compat"
    ]
  },
  "createdAt": "2026-03-13T18:25:10.033Z",
  "updatedAt": "2026-03-16T18:46:44.322Z",
  "health": "on-track"
}