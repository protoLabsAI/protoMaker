# actionlint configuration.
# Declares custom self-hosted runner labels so actionlint doesn't flag them as
# unknown. namespace-profile-protolabs-linux is the fleet's Namespace runner.
self-hosted-runner:
  labels:
    - namespace-profile-protolabs-linux
