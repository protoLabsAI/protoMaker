#added by trueheads > will remove once supercombo adds multi-os support
launch.sh

# Dependencies
node_modules/

# Build outputs
dist/
build/
out/
storybook-static/
docs/.vitepress/dist/
docs/.vitepress/cache/
.next/
.turbo/

# Automaker - runtime artifacts and user-generated content
.automaker/**/agent-output.md
.automaker/**/images/
.automaker/**/authority/
.automaker/**/analysis.json
.automaker/**/*.bak*
.automaker/.backups/
.automaker/events/
.automaker/execution-state.json
.automaker/notifications.json
.automaker/categories.json
.automaker/context/context-metadata.json
.automaker/worktrees/
.automaker/worktree-init.sh
.automaker/prd-output.json

# Feature data is runtime-managed by the server — never git-track
.automaker/features/

# Runtime data — never git-track
.automaker/ledger/
.automaker/recovery/
.automaker/checkpoints/
.automaker/actionable-items.json
.automaker/content/

# Instance-specific — each machine starts fresh
.automaker/projects/
.automaker/settings.json

# Track shared knowledge (agent learning, skills, context, spec)
!.automaker/context/
!.automaker/skills/
!.automaker/memory/
!.automaker/spec.md

.worktrees/
worktrees/

/logs
# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS-specific files
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# IDE/Editor configs
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# Editor backup/temp files
*~
*.bak
*.backup
*.orig
*.swp
*.swo
*.tmp
*.temp

# Local settings (user-specific)
*.local.json

# Application state/backup
backup.json

# Test artifacts
test-results/
coverage/
.nyc_output/
*.lcov
playwright-report/
blob-report/

# Environment files (keep .example)
.env
.env.local
.env.*.local
!.env.example
!.env.local.example

# Codex config (contains API keys)
.codex/config.toml

# TypeScript
*.tsbuildinfo

# Misc
*.pem

docker-compose.override.yml
.claude/docker-compose.override.yml
.claude/hans/

pnpm-lock.yaml
yarn.lock
packages/mcp-server/package-lock.json

# Fork-specific workflow files (should never be committed)
# API key files
data/.api-key
data/credentials.json
data/
!site/data/
.codex/
packages/mcp-server/plugins/automaker/.env

# Plugin metadata files
**/.orphaned_at

# Beads — instance-specific operational state, fresh per machine
.beads/

# Embedded git repos (should be submodules if needed)
examples/
# Ephemeral test directories
test/*-test-*/

# Labs directory - cloned repositories for testing/development
labs/

# Local project plans (not repo content)
/projects/

# Gource visualization (render locally, don't commit)
gource.mp4
.gource/

# Test fixture runtime artifacts
test/fixtures/**/.automaker/categories.json
