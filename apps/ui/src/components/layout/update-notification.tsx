/**
 * Update notification stub.
 * Desktop auto-updater was removed with Electron. Web updates happen via
 * the service worker (PWA) — see use-pwa-web.ts.
 */
export function UpdateNotification() {
  return null;
}
