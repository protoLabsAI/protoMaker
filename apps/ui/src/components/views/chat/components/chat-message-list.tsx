/**
 * ChatMessageList — Re-exports the shared ChatMessageList from @protolabs-ai/ui/ai.
 *
 * Scrollable message container with auto-scroll behavior, maintained in the
 * shared library for reuse across all chat surfaces.
 */

export { ChatMessageList } from '@protolabs-ai/ui/ai';
