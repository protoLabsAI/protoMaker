/**
 * Centralized port configuration for AutoMaker
 *
 * Re-exports from @protolabs-ai/types for backward compatibility.
 * The canonical definition is in @protolabs-ai/types to allow browser-safe imports.
 */

export { STATIC_PORT, SERVER_PORT, RESERVED_PORTS } from '@protolabs-ai/types';
