/**
 * Centralized port configuration for AutoMaker
 *
 * Re-exports from @protolabsai/types for backward compatibility.
 * The canonical definition is in @protolabsai/types to allow browser-safe imports.
 */

export { STATIC_PORT, SERVER_PORT, RESERVED_PORTS } from '@protolabsai/types';
