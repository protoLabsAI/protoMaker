{
  "name": "@@PROJECT_NAME",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/server & npm run dev --workspace=packages/ui",
    "build": "npm run build --workspace=packages/tools && npm run build --workspace=packages/tracing && npm run build --workspace=packages/prompts && npm run build --workspace=packages/flows && npm run build --workspace=packages/server && npm run build --workspace=packages/mcp",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
